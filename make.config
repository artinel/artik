SHELL ?= /bin/bash
CC ?= gcc
ASM ?= gcc
LD ?= ld
CFLAGS ?= -ffreestanding -nostdlib -static -O0 -g -std=gnu11 -Wall -Wextra -I./include
ASMFLAGS ?= $(CFLAGS)
LDFLAGS ?=
ARCH ?= x86
TARGET ?= artik-$(ARCH)
