SHELL ?= /bin/bash
CC ?= gcc
ASM ?= gcc
LD ?= ld
CFLAGS ?= -ffreestanding -nostdlib -static -O2 -g -std=gnu11 -Wall -Wextra -I./include -fstack-protector-all
ASMFLAGS ?= $(CFLAGS)
LDFLAGS ?=
ARCH ?= x86
TARGET ?= artik-$(ARCH)
